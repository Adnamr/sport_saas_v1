# Production profile configuration
spring:
  jpa:
    show-sql: false

  flyway:
    clean-disabled: true

logging:
  level:
    root: WARN
    com.sportsaas: INFO
    org.springframework.web: WARN
    org.hibernate.SQL: WARN
